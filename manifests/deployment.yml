apiVersion: apps/v1
kind: Deployment
metadata:
  name: be-deploy
spec:
  replicas: 3
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
      - name: backend
        image: acrforaksanhnt315.azurecr.io/tranhnguyenbego
        ports:
        - containerPort: 8080
        


